<div class="meta">
  <%= post.published_at.strftime("%B %d, %Y") %>
  <% if post.tags.any? %>
    - <%= raw post.tags.map { |t| link_to t.name, tag_path(t) }.join(", ") %>
  <% end %>
  - <%= link_to "permalink", permalink(post) %>
</div>
