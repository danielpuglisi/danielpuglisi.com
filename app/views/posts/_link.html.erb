<article class="post link">
  <header>
    <h2><%= link_to raw(post.title), post.external_link %> &rarr;</h2>
    <%= render "posts/meta", post: post %>
  </header>
  <%= markdown(liquidize(post.content)) %>
  <%= render "disqus" %>
  <%= render "posts/footer" %>
</article>
